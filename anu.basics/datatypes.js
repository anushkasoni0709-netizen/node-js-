"use strict"; //tis will treat all code in newer version of js

// alert(3+3).  we are using node js not browser

console.log(3+3);

let name="ANUSHKA"
let age = 20
let isLoggedIn = true
let state = null

console.table([name,state,isLoggedIn,state]);

console.log(typeof age);
console.log(typeof null);

